import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const UserNotifs = () => {
  return (
    <View>
      <Text>UserNotifs</Text>
    </View>
  )
}

export default UserNotifs

const styles = StyleSheet.create({})